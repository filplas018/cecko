
/*char ** code;
char*** mask;

mask = (char***)malloc(sizeof(char **));

for (int i = 0; i < N; i++) {
	mask[i] = (char**)malloc(sizeof(char *));
}

for (int i = 0; i < N; i++) {
	mask[i][j] = &code[i][j]
}
mask[i][j] = code[i][j];
*/